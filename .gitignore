# --- secrets (never commit) ---
# Secrets are managed in Doppler (project: free-plaud), not in env files. These
# patterns stay as defense-in-depth in case a local .env is ever created.
.plaud_env
*.env
.doppler.yaml

# --- private audio + generated outputs (keep meeting content off GitHub) ---
audio/
processed/
*.ogg
*.mp3
*.m4a
*.wav
*.opus
*.aac
*.flac
untranscribed_manifest.csv

# --- internal handoff prompt (transient, local only) ---
CLAUDE_CODE_HANDOFF.md

# --- logs / caches ---
*.log
__pycache__/
*.pyc
.DS_Store
